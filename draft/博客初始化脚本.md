```
# 创建网络
docker create network share-network

# 运行mysql
docker run -d -p 3306:3306 --name mariadb  --network=share-network -e MYSQL_ROOT_PASSWORD=blogdb123aaa -v /opt/data/mysql:/var/lib/mysql mariadb

# 运行博客程序后台api
docker run -d -p 8888:8888 --name service-blog --network=share-network  -e MYSQL_ROOT_PASSWORD=blogdb123aaa -e GITHUB_WEBHOOK_TOKEN=5656e13a2c90898ca8c54fd4eed99a6e53ba4e14 -v /opt/data/blog:/Users/qping/Documents/blog docker.qping.me:5000/service-blog:v2.0

# 运行博客前台界面

```
